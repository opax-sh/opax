// Package memory implements the built-in memory plugin for cross-platform
// agent context persistence. This is the primary value plugin — it enables
// context to flow between agent sessions across platforms.
package memory
