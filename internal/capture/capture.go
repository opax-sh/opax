// Package capture implements the passive capture engine that reads
// agent-native session storage after the fact. Agent-specific sub-packages
// handle each platform's session format.
package capture
